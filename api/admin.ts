import {db,requireAdmin} from './_db';export default async function handler(req:any,res:any){if(!requireAdmin(req,res))return;const sql=db();try{if(req.method==='GET'){const [bookings,reviews,media,invoices,turnoverRows,visitorRows,traffic,monthlyRevenue]=await Promise.all([sql`select * from bookings order by created_at desc limit 200`,sql`select * from reviews order by created_at desc limit 200`,sql`select * from media order by created_at desc limit 200`,sql`select * from invoices order by created_at desc limit 200`,sql`select coalesce(sum(total) filter(where created_at>=now()-interval '24 hours'),0) as h24,coalesce(sum(total) filter(where created_at>=now()-interval '14 days'),0) as d14,coalesce(sum(total) filter(where created_at>=now()-interval '30 days'),0) as d30,coalesce(sum(total) filter(where created_at>=date_trunc('year',now())),0) as yearly from invoices where status in ('Paid','Part paid')`,sql`select count(distinct visitor_hash) filter(where created_at>=date_trunc('day',now())) as daily,count(distinct visitor_hash) filter(where created_at>=date_trunc('month',now())) as monthly,count(*) filter(where created_at>=date_trunc('day',now())) as views_daily,count(*) filter(where created_at>=date_trunc('month',now())) as views_monthly from page_views`,sql`with days as (select generate_series(current_date-13,current_date,'1 day')::date d) select d,coalesce(count(distinct visitor_hash),0)::int visitors from days left join page_views on page_views.created_at>=d and page_views.created_at<d+1 group by d order by d`,sql`with months as (select generate_series(date_trunc('month',current_date)-interval '11 months',date_trunc('month',current_date),'1 month') m) select m,coalesce(sum(total),0)::numeric revenue from months left join invoices on invoices.created_at>=m and invoices.created_at<m+interval '1 month' and invoices.status in ('Paid','Part paid') group by m order by m`]);const approved=reviews.filter((x:any)=>x.status==='approved'),turnover=turnoverRows[0]||{},visitors=visitorRows[0]||{};const activity=[...bookings.map((x:any)=>({title:'Booking received',detail:`${x.booking_id} · ${x.trip}`,created_at:x.created_at})),...reviews.map((x:any)=>({title:'Review submitted',detail:`${x.name} · ${x.status}`,created_at:x.created_at})),...media.map((x:any)=>({title:`Visitor ${x.type} uploaded`,detail:`${x.trip} · ${x.status}`,created_at:x.created_at})),...invoices.map((x:any)=>({title:'Invoice saved',detail:`${x.invoice_number} · ${x.status}`,created_at:x.created_at}))].sort((a,b)=>+new Date(b.created_at)-+new Date(a.created_at)).slice(0,8);return res.status(200).json({metrics:{bookings:bookings.length,pending:bookings.filter((x:any)=>!['Confirmed','Completed','Cancelled'].includes(x.status)).length,rating:approved.length?approved.reduce((s:number,x:any)=>s+Number(x.rating),0)/approved.length:0,reviewCount:approved.length,turnover:{h24:Number(turnover.h24),d14:Number(turnover.d14),d30:Number(turnover.d30),yearly:Number(turnover.yearly)},visitors:{daily:Number(visitors.daily),monthly:Number(visitors.monthly),viewsDaily:Number(visitors.views_daily),viewsMonthly:Number(visitors.views_monthly)}},bookings,invoices,activity,traffic,monthlyRevenue})}if(req.method==='POST'&&req.body?.type==='invoice'){const i=req.body.invoice;const rows=await sql`insert into invoices(invoice_number,invoice_date,due_date,booking_ref,customer_name,customer_address,phone,email,items,tax_label,tax_rate,discount,subtotal,tax,total,status,notes,payment_details,travel_details) values(${i.invoice_number},${i.invoice_date||null},${i.due_date||null},${i.booking_ref||null},${i.customer_name},${i.customer_address||null},${i.phone||null},${i.email||null},${JSON.stringify(i.items)}::jsonb,${i.tax_label||'GST'},${i.tax_rate||0},${i.discount||0},${i.subtotal||0},${i.tax||0},${i.total||0},${i.status||'Draft'},${i.notes||null},${i.payment_details||null},${JSON.stringify(i.travel_details||{})}::jsonb) returning *`;return res.status(201).json({ok:true,invoice:rows[0]})}return res.status(405).json({error:'Method not allowed'})}catch(error){return res.status(500).json({error:error instanceof Error?error.message:'Admin request failed'})}}
