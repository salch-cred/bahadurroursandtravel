import {createHash} from 'node:crypto';import {db,clean} from './_db';export default async function handler(req:any,res:any){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{const sql=db(),b=req.body||{},raw=clean(b.visitorId,160);if(!raw)return res.status(400).json({error:'Visitor id required'});const salt=process.env.ANALYTICS_SALT||process.env.ADMIN_API_TOKEN||'bahadur';const visitorHash=createHash('sha256').update(raw+salt).digest('hex');await sql`insert into page_views(visitor_hash,path,referrer,user_agent) values(${visitorHash},${clean(b.path,300)||'/'},${clean(b.referrer,500)||null},${clean(req.headers['user-agent'],500)||null})`;return res.status(204).end()}catch{return res.status(204).end()}}
