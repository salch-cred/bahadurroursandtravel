import {db,clean} from './_db.js';export default async function handler(req:any,res:any){const sql=db();try{if(req.method==='GET'){const reviews=await sql`select name,trip,rating,text,media_url,media_type,created_at from reviews where status='approved' order by created_at desc limit 60`;const media=await sql`select type,guest_name,trip,caption,url,mime_type,created_at from media where status='approved' order by created_at desc limit 100`;return res.status(200).json({reviews,photos:[...media.filter((x:any)=>x.type==='photo'),...reviews.filter((x:any)=>x.media_type==='image').map((x:any)=>({type:'photo',guest_name:x.name,trip:x.trip,caption:x.text,url:x.media_url,created_at:x.created_at}))],videos:[...media.filter((x:any)=>x.type==='video'),...reviews.filter((x:any)=>x.media_type==='video').map((x:any)=>({type:'video',guest_name:x.name,trip:x.trip,caption:x.text,url:x.media_url,mime_type:'video/mp4',created_at:x.created_at}))]})}if(req.method==='POST'){req.query={};return res.status(307).setHeader('Location','/api/reviews').end()}return res.status(405).json({error:'Method not allowed'})}catch(error){return res.status(500).json({error:error instanceof Error?error.message:'Community request failed'})}}
