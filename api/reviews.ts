import { put } from '@vercel/blob';import {db,clean} from './_db';
export default async function handler(req:any,res:any){const sql=db();try{if(req.method==='GET'){const slug=clean(req.query?.package,160);const limit=Math.min(30,Math.max(1,Number(req.query?.limit)||8));const rows=slug?await sql`select name,trip,rating,text,media_url,media_type,created_at from reviews where status='approved' and (package_slug=${slug} or package_slug is null) order by created_at desc limit ${limit}`:await sql`select name,trip,rating,text,media_url,media_type,created_at from reviews where status='approved' order by created_at desc limit ${limit}`;return res.status(200).json({reviews:rows})}if(req.method==='POST'){const b=req.body||{};if(!b.consent)return res.status(400).json({error:'Publication consent is required'});let media_url=null,media_type=null;if(b.data&&b.mimeType){if(!process.env.BLOB_READ_WRITE_TOKEN)return res.status(503).json({error:'Visitor media storage is not configured'});const buffer=Buffer.from(String(b.data),'base64');if(buffer.length>3*1024*1024)return res.status(400).json({error:'Media must be smaller than 3 MB'});const allowed=['image/jpeg','image/png','image/webp','video/mp4','video/webm'];if(!allowed.includes(b.mimeType))return res.status(400).json({error:'Unsupported media type'});const blob=await put(`reviews/${Date.now()}-${clean(b.fileName,120)}`,buffer,{access:'public',contentType:b.mimeType});media_url=blob.url;media_type=String(b.mimeType).startsWith('video/')?'video':'image'}const row={name:clean(b.name,120),trip:clean(b.trip,160),rating:Math.max(1,Math.min(5,Number(b.rating)||5)),booking_ref:clean(b.bookingRef,80).replace(/^#/,'').toUpperCase(),package_slug:clean(b.packageSlug,160)||null,text:clean(b.text,1200),media_url,media_type,consent:true};if(!row.name||!row.trip||!row.booking_ref||!row.text)return res.status(400).json({error:'Complete all required fields'});const booking=await sql`select booking_id from bookings where upper(booking_id)=${row.booking_ref} limit 1`;const status=booking.length?'approved':'pending';await sql`insert into reviews(name,trip,rating,booking_ref,package_slug,text,media_url,media_type,consent,status) values(${row.name},${row.trip},${row.rating},${row.booking_ref},${row.package_slug},${row.text},${row.media_url},${row.media_type},true,${status})`;return res.status(201).json({ok:true,status})}return res.status(405).json({error:'Method not allowed'})}catch(error){return res.status(500).json({error:error instanceof Error?error.message:'Review request failed'})}}
