document.querySelectorAll('img:not(.hero-bg)').forEach(img=>{if(!img.loading)img.loading='lazy';img.decoding='async'});document.querySelectorAll('video').forEach(v=>{v.preload='metadata';v.playsInline=true});const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('revealed');observer.unobserve(e.target)}}),{rootMargin:'80px'});document.querySelectorAll('main>section,.package-card').forEach(el=>{el.classList.add('reveal-ready');observer.observe(el)});
try{const idKey='bahadur-visitor-id',id=localStorage.getItem(idKey)||crypto.randomUUID();localStorage.setItem(idKey,id);const path=location.pathname||'/';const hitKey='bahadur-hit-'+path,last=Number(sessionStorage.getItem(hitKey)||0);if(Date.now()-last>30*60*1000){sessionStorage.setItem(hitKey,String(Date.now()));fetch('/api/community?type=analytics',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({visitorId:id,path,referrer:document.referrer}),keepalive:true}).catch(()=>{})}}catch{}
